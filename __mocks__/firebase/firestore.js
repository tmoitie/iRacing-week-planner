export const getFirestore = jest.fn(() => ({}));

export const collection = jest.fn(() => '654321');

export const doc = jest.fn(() => '123456');

export const getDoc = jest.fn(async () => ({}));

export const setDoc = jest.fn(async () => {});
