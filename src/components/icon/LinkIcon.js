import React from 'react';
import styles from '../../styles/main.module.scss';

export default function LinkIcon() {
  return <span className={`${styles.glyphicon} ${styles['glyphicon-link']}`} />;
}
